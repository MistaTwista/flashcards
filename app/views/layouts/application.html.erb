<!DOCTYPE html>
<html>
<head>
  <title><%= t( "name" ) + yield( :title ) %></title>
  <%= render "shared/html_header" %>
</head>
<body>
  <%= render "shared/menu" %>
  <div class="container">
    <%= yield %>
  </div>
  <footer class="footer">
    <div class="container">
      <p class="text-muted">&copy;&nbsp;<%= t( "name" ) + " " + Time.now.year.to_s %></p>
    </div>
  </footer>
</body>
</html>
