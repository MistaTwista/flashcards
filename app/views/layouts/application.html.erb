<!DOCTYPE html>
<html>
<head>
  <title><%= t( "name" ) + yield( :title ) %></title>
  <%= render "shared/html_header" %>
</head>
<body>

<%= render "shared/menu" %>
<div class="container">
  <%= yield %>
</div>
<footer class="footer">
  <div class="container">
    <p class="text-muted">Place sticky footer content here.</p>
  </div>
</footer>
</body>
</html>
