<%= simple_form_for :user_sessions, url: user_sessions_path, method: 'post' do |f| %>
  <%= f.input :email, autofocus: true, :input_html => { :name => :email } %>
  <%= f.input :password, :input_html => { :name => :password } %>
  <%= f.button :submit %>
<% end %>

